# 2026-09-23 发布验证

业务代码版本：`7a4f9fc`。本轮中等级修复与此前已验证的本地优化一同部署。

## 完成

- 本地和服务器 Linux 隔离环境均通过 98 项测试；自动测试使用临时数据库和模拟 Telegram 客户端。
- 桌面与手机两套界面回归通过，无页面脚本异常。
- 部署前制作生产数据库副本，在副本上重复运行初始化/兼容迁移两次；账号、任务、队列、联系记录数保持不变，完整性与外键检查通过。
- 正式服务停止后备份旧代码、数据库和加密环境配置，再更新程序。环境配置未被替换。
- 旧库转发目标字段的允许留空迁移成功；没有增加本轮新索引、调整数据库池或 WAL。
- 发布后健康检查成功；两个此前在线账号重新连接，原有待登录账号保持待登录。
- 正式库仍为 3 个账号、1 个任务、152 条队列记录、24 条联系记录（验证时快照）；数据库 quick_check 为 ok。
- 服务器运行代码与发布包逐文件比较，无差异。
- 实际网页登录及六个页面检查通过，HTTP 200，页面异常 0；业务运行状态保持运行。
- 本次启动后未检测到 ERROR/Traceback 日志标记，未发生自动重启。短期健康检查不能代表长期稳定性。
- 原有 telegram-forwarder 服务保持 active，进程 ID 与部署前一致。

## 备份与回退

服务器受限备份目录：`/var/backups/telegram-ops/release-7a4f9fc`。包含 code-before.tar.gz、before.db、配套 .env 和初始访问配置；备份不加入 Git。

代码回退应停止本服务，恢复旧代码包并重新启动。数据库备份仅用于经过评估的恢复操作；上线后可能产生新记录，不可盲目覆盖当前库，否则会丢失发送/去重记录。已交付消息无法通过恢复备份撤回。

## GitHub 同步

用户指定目标：`https://github.com/beizhe60-ops/-.git`。已在用户明确确认权限后，通过 Chrome 完成 GitHub 官方 CLI 登录，并将完整代码及提交历史推送到该仓库的 `main` 分支。首次同步提交为 `888fe95`，包含部署业务版本 `7a4f9fc`。随后补充本发布记录的同步结果。运行数据库、Telegram 会话、环境密钥和服务器备份未加入版本控制。原作者仓库保留为 `upstream`，用户仓库设为 `origin`。

## 尚未完成

真实 Telegram 完整 A→转发→B 私信测试、24–72 小时持续运行/断线压力验证、大文件拆分、旧业务字段整理、索引和历史保留策略。本次没有发送人工测试消息，不将账号恢复在线等同于真实链路验收。
