# Telegram Ops 中文工作台

基于 [kevenlemon/telegram-ops](https://github.com/kevenlemon/telegram-ops) 开发，保留原项目 MIT 许可证。

## 本版实现

- 中文网页工作台：运行概览、账号管理、任务与过滤、消息记录、不再联系、系统设置。
- 监测账号与私信账号分开管理；手机号、验证码及二级登录密码登录。API 凭据在系统设置统一配置。
- 每个监测账号配置完整群组 ID；后台同时校验账号范围与任务来源，精准限制监测。
- 任务绑定独立 A/B 账号、来源群、中转群、关键词、排除词、忽略用户、私信文案。
- A 捕获符合条件的实际发言者，发送 `群组-该用户发言的内容-@用户名`。
- B 只处理指定 A 发出的标准消息，提取末尾用户名，解析目标用户后自动私信。
- 无用户名、机器人及自身账号跳过；按用户 ID 跨任务去重；发送前检查不再联系名单。
- 持久化队列、平台等待、暂停/取消、失败记录。发送结果不确定时停止自动重试。
- 不发送的模拟测试；同一实际规则引擎生成过滤结果、中转格式和私信预览。
- 后台密码修改、密码哈希、加密账号会话、同源请求检查、独立初始密码。

任务初建或修改后处于暂停状态；修改任务会取消该任务旧的待发送记录。中转消息按纯文本发送，换行会折叠为空格，正文中其他 @提及不作为收件人。

## 本机启动

需要 Python 3.12。

```sh
python3.12 -m venv .venv
.venv/bin/pip install -r telegram-ops/requirements.txt
cd telegram-ops
# 首次部署运行。已有自己的 .env 时不要覆盖。
../.venv/bin/python scripts/setup_console.py
../.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8765 --workers 1
```

打开 http://127.0.0.1:8765/console。
初始管理员账号和随机密码保存在 `telegram-ops/.local-access.json`；该文件不加入版本控制。管理员也可在系统设置修改密码，修改后旧会话失效，此文件不会同步更新。

当前开发目录已生成独立本机配置，不需要再次运行 setup_console.py。

## Linux Docker 部署

使用当前源码构建，不依赖上游镜像，不启用 privileged。

1. 安装 Docker 与 Compose。
2. 生成 `.env` 和登录凭据（可在本机生成后安全传到服务器，不要提交到 Git）。
3. 在项目根目录执行：

```sh
docker compose up -d --build
docker compose logs -f --tail=100
```

服务默认仅绑定服务器 `127.0.0.1:8000`。远程访问用 SSH 端口转发，或通过自己的域名配置 HTTPS 反向代理；HTTPS 环境设置 `ADMIN_COOKIE_SECURE=true`。

```sh
ssh -L 8000:127.0.0.1:8000 your-server
```

然后浏览器打开 http://127.0.0.1:8000/console。

数据库及加密后的账号会话存储在 `console-data` 数据卷，`.env` 内的密钥必须与数据一同妥善备份。后台设置保存在数据库；环境配置负责服务级凭据和网络设置。

本版只支持单应用进程，**不要增加 uvicorn workers 或容器副本数**。同一 SQLite 数据库有进程锁，队列按单进程设计。

## 网页操作顺序

1. 在系统设置配置一次 Telegram API ID / API Hash（加密保存）；然后分别进入“监测账号”和“私信账号”，填写手机号、获取验证码并登录，已开启两步验证的账号还需填写二级登录密码。
2. 用 Telegram 加入来源群。A、B 都加入中转群；A 在中转群需要发消息权限，B 需要读取权限。
3. 在监测账号卡片填写完整监测群组 ID（例如 `-1001234567890`），每行一个；未设置时不监测来源群。分别同步 A、B 群组，打开所需发送开关。
4. 新建任务，勾选 A 来源群、选择共同中转群、设置关键词及文案。
5. 用模拟测试检查过滤和格式，保存并启用任务。
6. 点击“启动服务”。只处理启动之后的新消息，不自动扫描历史消息。
7. 在消息记录查看结果。发送失败不会无限重试；不确定状态保留去重记录，避免重复联系。

移除监测群组 ID 时，受影响任务会暂停，其旧待发送消息会取消。已有账号升级时会从原任务迁移监测范围。修改系统 API 凭据只用于后续新增账号，已有账号保留创建时的凭据。

服务暂停状态会持久化。服务运行时服务器重启，会恢复连接及已排队的待发送消息，但不保证补齐停机期间群里的消息。Telegram 群组用于事件传递，不是保证不丢失的任务队列。

## 测试

```sh
.venv/bin/pip install -r telegram-ops/requirements-dev.txt
cd telegram-ops
../.venv/bin/python -m pytest -q
```

测试使用隔离的临时数据库和模拟 Telegram 客户端，不需要真实账号，不会发消息。覆盖 A/B 流转、用户去重、错误收件人防护、平台等待、重启处理、认证、配置校验及密码更改。

## 当前边界

- 尚未用真实 A/B 账号联调；需要你在网页登录并用自己的测试群和测试收件人验证。
- 未部署到远程服务器，Docker 配置已提供，当前环境未验证 Docker 镜像构建。
- 不含批量账号轮换、自动加群、AI 判断、外部通知、网页备份恢复、记录定时清理。
- API 错误、账号限制和收件人设置仍可能导致发送失败；平台等待会按返回时间暂停。
- 上游旧页面/规则代码保留用于溯源；新的后台服务只执行 A/B 任务，不执行旧规则任务。
- 生产上线前仍需结合实际服务器进行 HTTPS、持久化与真实账号验收。
