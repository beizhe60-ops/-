# 长期开发规则

## 文档与范围

- 本文件仅记录长期开发规则和禁止事项。产品需求、业务规则、架构目标和验收标准统一维护在 `docs/ARCHITECTURE_REQUIREMENTS.md`。
- 修改前先阅读适用的需求与现有实现；区分用户已确认的需求、当前实现、重构目标和待确认事项，不把建议自动视为已批准的业务变更。
- 遵循上级目录的约束；`sources/` 中的同步参考材料只读，禁止编辑、移动、重命名或删除。
- 保持改动范围与当前任务一致。文档任务不修改业务代码；代码重构不夹带未经要求的功能、视觉重设计或生产环境操作。
- 临时状态、密码、服务器地址、进程号、测试次数和部署结果不写入本文件。

## 结构与兼容性

- 分离页面交互、接口校验、业务规则、持久化、Telegram 连接与后台任务执行；避免在单个模块持续叠加职责。
- 每类业务配置明确唯一事实来源；派生展示从该来源计算，禁止用多处独立保存的配置互相猜测或覆盖。
- 身份和路由使用稳定标识，展示名称不能代替业务标识。前端校验不能代替服务端校验。
- 保持业务行为和数据兼容；数据库变更通过可重复执行的迁移完成，说明旧数据处理方式和回退路径。
- 兼容层必须明确用途、适用范围与移除条件；禁止用没有说明的占位值掩盖数据模型问题。
- 不为拆分而拆分，不未经评估引入微服务、额外数据库、消息中间件或多实例部署。

## 并发、消息与错误处理

- 将事件接收、持久化排队、账号分配、发送和结果记录分开；关键状态变化必须具备事务边界及重复执行保护。
- 明确配置变更、暂停、解绑与正在执行任务之间的关系；发送前重新验证当前权限和绑定，不能仅依赖旧快照。
- 重试必须区分可重试失败、平台等待、永久失败及结果不确定；禁止无限重试或对结果不确定的发送盲目重发。
- 不通过轮换账号绕过平台等待或限制；账号故障应隔离，不能导致其他账号或网页接口长期阻塞。
- 不承诺无法保证的“绝不重复”或“绝不丢消息”；对外部系统与本地提交之间的不确定性保留可查询状态。
- 错误必须可观察、可定位，不能静默吞掉业务失败；日志不得包含登录秘密或无关的私人内容。

## 凭据与环境

- 不将 SSH 密码、后台密码、API Hash、令牌、验证码、二级密码、会话文件、生产数据库或含密钥的备份写入源码、文档、测试夹具或版本控制。
- 验证码和二级密码仅用于必要的认证过程，不持久化、不回显；必须保留的应用密钥和会话使用受控加密存储。
- 不在页面、接口响应、日志或错误详情中泄露秘密；不能用真实凭据作为默认配置。
- 开发、测试和生产环境隔离；测试默认使用临时数据库、模拟 Telegram 客户端和虚构数据，不向真实用户发送消息。
- 不因普通代码或文档任务自行部署、重启、修改生产配置。已明确授权的部署按授权范围执行，不重复索取相同授权。
- 生产变更保护现有服务、数据和备份；禁止覆盖无关机器人目录、数据库、凭据或服务配置。

## 验证与交付

- 按实际风险选择验证：业务变更覆盖路由、边界、幂等、故障和迁移；界面变更检查真实浏览器操作、输入保留及窄屏布局。
- 测试验证业务结果而非复制实现；不为文档、简单样式或其他低影响修改添加无意义测试。
- 只有证据支持时才报告测试、部署或真实联调成功；明确自动化测试、浏览器模拟、服务器检查和真实 Telegram 验证的区别。
- 交付说明改了什么、为何修改、如何验证以及尚未验证的边界；相关需求和操作文档随实际行为更新。
- 不承诺“零 bug”；通过清晰状态、故障测试、可观察性、备份与回退降低风险。
